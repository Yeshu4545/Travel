# Proxy /api → local Node backend (run: cd server && npm start)
REACT_APP_PROXY_TARGET=http://127.0.0.1:5000

# Share links use this URL (not localhost) so others can open them
REACT_APP_PUBLIC_APP_URL=https://travel-bice-six.vercel.app
